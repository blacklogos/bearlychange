node_modules/
.env
.DS_Store

# Per-machine symlink to the user's shared lessons store
# (`ln -s ~/Dropbox/Knowledge/Lessons docs/solutions`).
docs/solutions

# Cloudflare Workers local state + secrets
.wrangler/
.dev.vars
.wrangler-secrets.local
